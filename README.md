To see the error just start the project:
`QUARKUS_PROFILE=staging ./mvnw clean compile quarkus:dev`

the URL is present in the application-staging.properties and overwritten in the applications.properties.